export interface IWindow extends Window {
    config: {
        API: string;
        BASE_URL: string;
        TERMS: string;
        SUBMIT_LINK: string;
        LEFT_TEAM_IMAGE: string;
        RIGHT_TEAM_IMAGE: string;
    };
}